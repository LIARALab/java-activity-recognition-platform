package org.liara.api.request

import spock.lang.Specification

class ImmutableAPIRequestParameterSpecificiation extends Specification
{
  def "plopl" () {
    given: "a consumer"
     
    and: "a consumer mixin"
      
    expect: "the mixin's publisher set must be empty"
      null == null
  }
}
