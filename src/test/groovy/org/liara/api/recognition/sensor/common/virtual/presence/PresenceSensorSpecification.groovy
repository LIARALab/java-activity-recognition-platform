package org.liara.api.recognition.sensor.common.virtual.presence

import org.liara.api.test.builder.node.NodeBuilder

import spock.lang.Specification

public class PresenceSensorSpecification extends Specification
{
  /*
  def "it emit a new unfinished presence when it discover it's first motion sensor tick" () {
    
    given: "an house node with some rooms and motion sensors"
      final Node house = NodeBuilder.createHouse {
        withRoom {
          withName "kitchen"
          withMotionSensor {
            withName "kitchen-motion-sensor"
          }
        }
        withRoom {
          withName "bedroom"
          withMotionSensor {
            withName "bedroom-motion-sensor"
          }
        }
        withRoom {
          withName "bathroom"
          withMotionSensor {
            withName "bathroom-motion-sensor"
          }
        }
      }
  }
  */
}
